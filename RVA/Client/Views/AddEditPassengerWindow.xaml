<Window x:Class="TravelSystem.Client.Views.AddEditPassengerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="First Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Padding="5"/>
        
        <Label Grid.Row="1" Grid.Column="0" Content="Last Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Padding="5"/>
        
        <Label Grid.Row="2" Grid.Column="0" Content="Passport Number:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PassportNumber, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Padding="5"/>
        
        <Label Grid.Row="3" Grid.Column="0" Content="Luggage Weight (kg):" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding LuggageWeight, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Padding="5"/>
        
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" 
                   Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Width="80" Height="30" 
                   Margin="0,0,10,0" Command="{Binding SaveCommand}" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Height="30" 
                   Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>